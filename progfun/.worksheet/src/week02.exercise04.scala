package week02

object exercise04 {
  
  class Rational(x: Int, y: Int){
   def num = x
   def den = y
  };import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(129); val res$0 = 
  
  new Rational(1,3);System.out.println("""res0: week02.exercise04.Rational = """ + $show(res$0))}
}
